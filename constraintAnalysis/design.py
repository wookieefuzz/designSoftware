class design:
    
    def __init__(self,AR,e,rho,etaP,etaM,LoDMax,RofC,vCruise,cd0,N,vHL,vMax,ClMax):
        self.AR = float(AR)
        self.e = float(e)
        self.rho = float(rho)
        self.etaP = float(etaP)
        self.etaM = float(etaM)
        self.LoDMax = float(LoDMax)
        self.RofC = float(RofC)
        self.vCruise = float(vCruise)
        self.cd0 = float(cd0)
        self.N = float(N)
        self.vHL = float(vHL)
        self.vMax = float(vMax)
        self.ClMax = float(ClMax)
        
        
        